import { PushNFTs } from "~/components/PushNFTs";

export default function PushNFTsPage() {
  return (
    <div className="flex flex-col items-center justify-center w-full h-full">
      <PushNFTs />
    </div>
  );
}